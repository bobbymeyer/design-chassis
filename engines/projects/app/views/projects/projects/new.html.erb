<%= page_head "Start a project", lede: "A name and a tag. What it gathers is whatever already carries the tag, so there is nothing to add afterwards." %>

<%= render "projects/projects/form", project: @project, submit_label: "Start it", cancel_to: projects_path %>
